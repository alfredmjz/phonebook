{
	"name": "phonebook",
	"version": "1.0.0",
	"description": "Add the phone number of your favorite person!",
	"main": "index.js",
	"scripts": {
		"start": "node index.js",
		"dev": "nodemon index.js",
		"test": "echo \"Error: no test specified\" && exit 1",
		"deploy": "git push heroku main",
		"deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy",
		"logs:prod": "heroku logs --tail",
		"lint": "eslint ."
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/alfredmjz/phonebook.git"
	},
	"author": "Alfred Mah",
	"license": "MIT",
	"devDependencies": {
		"eslint": "^8.17.0",
		"nodemon": "^2.0.16"
	},
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.0.1",
		"express": "^4.18.1",
		"mongoose": "^6.3.5",
		"morgan": "^1.10.0"
	}
}

